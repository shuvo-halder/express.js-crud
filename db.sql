CREATE DATABASE bookexpress;

CREATE table book (
    id varchar(255) primary key,
    name varchar(255),
    description varchar(255)
);

insert into book(id, name, description) values (id, name, description);